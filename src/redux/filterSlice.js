// import { createSlice } from "@reduxjs/toolkit";
// import { UseSelector } from "react-redux/es/hooks/useSelector";
// import { contactReduce } from "./contactSlice";

// const filterSlice = createSlice ({
//     name: 'filter',
//     initialState: '',
//     reducers: {
//         filterFindContact(state, action) {
//             // contactReduce.filter(contact => contact.name.toLowerCase().includes(action.payload.toLowerCase()))
//             state = action.payload;
//         }
//     }
// })

// export const { filterFindContact} = filterSlice.actions;
// export const filterReducer = filterSlice.reducer;